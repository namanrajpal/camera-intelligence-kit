# Secrets — never commit
.env
.env.*
*.key
keys.md
auth.json

# Godot
.godot/
.import/
export.cfg
export_presets.cfg
*.translation

# Node / browser bridge
node_modules/
dist/
build/
*.log
npm-debug.log*

# ML models / weights (large binaries — track via LFS or release assets if needed)
*.onnx
*.tflite
*.pte
*.task
*.bin

# GDMP native binaries (large, platform-specific — clone from GDMP-demo)
addons/GDMP/libs/
# CameraServerExtension native binaries (same source)
addons/CameraServerExtension/arm32/
addons/CameraServerExtension/arm64/
addons/CameraServerExtension/x86_32/
addons/CameraServerExtension/x86_64/
addons/CameraServerExtension/wasm32/
addons/CameraServerExtension/*.xcframework/

# Python / ML training (YOLO, venv, datasets — large, local-only)
.venv/
runs/
datasets/
*.pt
*.pth
wandb/
__pycache__/
*.pyc

# Benchmark artifacts (published separately with blog post release)
corpus/
benchmark_results/
models/rtmpose/*.zip
models/rtmpose/rtmpose-s-body/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.tmp
